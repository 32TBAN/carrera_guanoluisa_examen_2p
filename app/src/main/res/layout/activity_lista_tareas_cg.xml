<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <RelativeLayout
        android:id="@+id/titleBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#3498db"
        android:padding="10dp">

        <!-- Resto del contenido del titleBar (íconos, título, etc.) -->

    </RelativeLayout>

    <LinearLayout
        android:id="@+id/tabs"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/titleBar"
        android:background="#f1f1f1"
        android:orientation="horizontal"
        android:padding="10dp">

        <!-- Cambié el atributo onClick por un Listener en el código Java -->
        <TextView
            android:id="@+id/tabPersonal"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:clickable="true"
            android:text="Personal"
            android:textSize="16sp"
            android:textColor="#000" />

        <TextView
            android:id="@+id/tabBusiness"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:clickable="true"
            android:text="Business"
            android:textSize="16sp"
            android:textColor="#000" />
    </LinearLayout>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/tabs">

        <!-- Contenido de la pestaña Personal -->
        <RecyclerView
            android:id="@+id/recyclerViewPersonal"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="gone" />

        <!-- Contenido de la pestaña Business -->
        <RecyclerView
            android:id="@+id/recyclerViewBusiness"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="gone" />

    </FrameLayout>

</RelativeLayout>
